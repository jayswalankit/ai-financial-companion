<configuration>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Current log file -->
        <file>logs/financial-companion.log</file>

        <!-- Rotation policy: size + time -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Rotated files pattern -->
            <fileNamePattern>logs/financial-companion-%d{yyyy-MM-dd}.%i.log</fileNamePattern>

            <!-- Max size per file -->
            <maxFileSize>10MB</maxFileSize>

            <!-- Keep last 10 days/files -->
            <maxHistory>10</maxHistory>

            <!-- (optional but good) total size cap -->
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Log levels -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- (optional) apne package ke liye DEBUG -->
    <logger name="com.aifinance.financialcompanion" level="DEBUG"/>

</configuration>